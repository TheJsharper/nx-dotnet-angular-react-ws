import { generatedWebapiTypes } from './generated-webapi-types';

describe('generatedWebapiTypes', () => {
  it('should work', () => {
    expect(generatedWebapiTypes()).toEqual('generated-webapi-types');
  });
});
